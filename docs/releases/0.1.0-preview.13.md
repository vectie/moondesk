# MoonDesk 0.1.0 Preview 13

This preview integrates MoonDesk with the exact MoonFind-to-MoonFlow execution
boundary while preserving MoonDesk as the human desktop and composition
surface.

## Changes

- Accepts only `moonsuite.work-model.v1` from the selected MoonBook and never
  lets a draft graph shadow executable work.
- Resolves a digest-pinned `moonflow.capability-catalog.v1`, validates the
  compiled graph, and imports only through MoonFlow's conformant import.
- Binds saved composition to graph identity, source digest, and declaration
  revision, with deterministic compiled identities and safely rebased
  suite-relative artifacts.
- Persists the catalog snapshot, validation report, and import receipt while
  leaving run events, projections, scheduling, recovery, and control mutations
  under MoonFlow ownership.
- Presents exact operation, schema, authority, claim, adapter, review, and
  health evidence without treating installed-product inventory as readiness.
- Removes product-specific continuation semantics and derives continuation
  from graph dependencies and MoonFlow runnable projections.
- Adds dynamic canvas bounds and a visible Fit control for the 63-stage
  MoonFind robotics reference.
- Persists validated canvas coordinates and makes each node move grip
  keyboard-operable with normal and accelerated arrow movement.
- Restores composition, run, and operator-action projections when a recovered
  session opens directly in Flow.
- Renders generic v2 manifest-owned handoffs and actions without adding
  product-domain branches to MoonDesk or MoonFlow.

## Compatibility

- macOS 14.0 or newer on Apple Silicon.
- Unsigned preview; not Developer ID signed or Apple-notarized.
- Local single-user alpha.

## Validation

- Backend integration-contract tests: 6 passed.
- Backend handler tests: 6 passed.
- Focused Rabbita workflow and canvas tests: 20 passed.
- MoonBit check and interface generation completed with zero errors.
- Production UI build passed.
- A later isolated UI-to-UI qualification visibly imported and rendered all 63
  stages across 11 products, exercised Fit/zoom/drag/pan/save, created run
  sequence 64, exposed one initial runnable item, and reported no browser
  console warnings/errors.
- The remediation rerun imported a canonical `book.toml` / `wiki.toml` book
  without fixture `book.json`, moved `understand-needs` from `64,72` to
  `72,104` through its keyboard grip, and recovered the exact coordinates
  after host restart.
- The recovered run displayed **ACTIONS READY**, one available
  **Open moonbook** action, and no contradictory unpublished-action fallback.
- The action projection does not prove stage execution: no configured
  receiving MoonBook app recorded a correlated receipt, the 63 domain stages
  were not executed, and the fixture catalog still does not establish live
  production capability health.

The remote preview workflow reruns the repository's mandatory full validation,
builds the unsigned macOS artifact, and verifies its immutable release
contract.

## Known release blockers

This preview is not a production-ready release. It still requires
Developer ID signing, notarization, Gatekeeper and clean-machine evidence,
update and rollback qualification, live host-generated capability health, and
the frozen lifecycle soak. It also requires a configured receiving-product
entrypoint, correlated receiver receipts, return-to-Flow recovery, and
end-to-end execution evidence before the 63-stage loop can qualify.
