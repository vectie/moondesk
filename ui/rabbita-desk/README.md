# Moondesk Rabbita UI

Browser-mode desktop shell for Moondesk. The shell includes Explorer, Search,
Inbox, Agents, Town, Runs, and Settings activities. The Agents activity talks to
the host `/api/agents/*` bridge for book-scoped MoonClaw sessions and projected
MoonClaw transcript/progress rows.

```bash
npm run dev
npm run build
```
